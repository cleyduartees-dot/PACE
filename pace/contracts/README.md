# Contracts

The versioned, checkable schema that any `.pace/` instance (or an Organización hub) must satisfy — the source of truth that `validator`, `doctor`, and `migrator` check against.

See [`INSTANCE_CONTRACT_0.1.0.pdl`](./INSTANCE_CONTRACT_0.1.0.pdl) — status DRAFT, built on top of [`ontology/PACE_ONTOLOGY_0.1.0.pdl`](../ontology/PACE_ONTOLOGY_0.1.0.pdl).
